-module(messagestore).
-include("messagestore.hrl").

%-behaviour(gen_server).

%start() -> 
%    gen_server:start_link({local, ?MODULE}, ?MODULE, [], []).

%init([]) ->
%    Store = dict:new(),
%    {ok, Store}.

%get_message() -> ok.
%add_message() -> ok.
%flag_message_for_deletion() -> ok.